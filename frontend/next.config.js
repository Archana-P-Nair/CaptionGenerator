/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    NEXT_PUBLIC_API_URL: "https://captiongenerator-j87m.onrender.com",
  },
};

module.exports = nextConfig;
